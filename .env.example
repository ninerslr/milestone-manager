# Copy this file to `.env` and fill in the real value. `.env` is git-ignored.
# This file is committed, so it holds fake values only.

# The Neon Postgres database. Use the POOLED connection string: its host
# name contains "-pooler".
DATABASE_URL="postgresql://user:password@ep-example-pooler.us-west-2.aws.neon.tech/neondb?sslmode=require"
